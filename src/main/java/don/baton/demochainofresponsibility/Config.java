package don.baton.demochainofresponsibility;

import don.baton.demochainofresponsibility.matcher.IdMatcher;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class Config {

//    @Bean
//    IdMatcher getDefaultMatcher() {
//        return (IdMatcher) new IdMatcher()
//                .addRule((p, b) -> p.sum > 0)
//                .addRule((p, b) -> b.sum > 0);
//    }
}
